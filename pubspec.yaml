name: modular_localization

version: 1.0.0

description: A tool for generating string localizations.

homepage: https://github.com/TiagoLacerda/modular-localization

repository: https://github.com/TiagoLacerda/modular-localization

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
# publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
